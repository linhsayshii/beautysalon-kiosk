{
  "name": "annachill-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "REST API for AnnaChill Salon Admin",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "find src -name '*.js' -print0 | xargs -0 -n1 node --check && node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "5.1.0",
    "pg": "8.16.3"
  }
}
